First public BlowTorch 2 release candidate for F-Droid.

Modern Android builds (SDK 36), scoped-storage import/export and zip backups, clearer Options (session log, battery helper, GMCP), growable input bar, notification sound picks, plugin refresh, Help and crash/log report, launcher connection duration.
